<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!--SpringMVC的配置文件， 包含网站跳转逻辑的控制，配置-->
    <context:component-scan base-package="com.controller"></context:component-scan>
    <mvc:annotation-driven/>
    <mvc:default-servlet-handler/>

    <!--配置视图解析器 视图解析器对象-->
    <bean id=" internalResourceViewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <!--配置控制器要返回给的成功项面的包路径-->
        <property name="prefix" value="/WEB-INF/pages/"/>
        <!--配置成功页面的后缀-->
        <property name="suffix" value=".jsp"/>
    </bean>

    <!--配置spring_mvc的文件上传解析器的bean对象-->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <!--文件上传的总大小-->
        <property name="maxUploadSize" value="1405600" />
        <!--上传单个文件的总大小-->
        <property name="maxUploadSizePerFile" value="524800" />
        <!--上传文件的编码类型-->
        <property name="defaultEncoding" value="UTF-8"/>
    </bean>


    <!--&lt;!&ndash;  自定义拦截器 拦截需要进行登录才能查看的界面 对登录进行令牌验证  &ndash;&gt;-->
    <mvc:interceptors>
        <mvc:interceptor>
            <!--代表要拦截的地址-->
<!--            <mvc:mapping path="/userInformation"/>-->
            <mvc:mapping path="/users"/>
            <mvc:mapping path="/userTrend"/>
            <mvc:mapping path="/userInformation"/>
            <mvc:mapping path="/managers"/>
<!--            <mvc:mapping path="user"/>-->
            <!--代表不需要token验证的方法，常见是登录告诉系统拦截的的位置（前面第三点的位置）-->
            <mvc:exclude-mapping path="/users/login"/>
            <mvc:exclude-mapping path="/users/register"/>
            <mvc:exclude-mapping path="/users/loginPage"/>
            <mvc:exclude-mapping path="/users/registerPage"/>
            <mvc:exclude-mapping path="/managers/login"/>
            <bean class="com.interceptor.TokenInterceptor"></bean>
        </mvc:interceptor>
    </mvc:interceptors>

</beans>